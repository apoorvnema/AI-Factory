import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pro.apoorvnema.aifactory',
  appName: 'AI Factory',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
